#include<stdio.h>
main()
{float side,area,perimeter;
printf("value for side is");
scanf("%f",&side);
area=side*side;
printf("area of square is %f\n",area);
perimeter=4*side;
printf("perimeter of square is %f",perimeter);
}
